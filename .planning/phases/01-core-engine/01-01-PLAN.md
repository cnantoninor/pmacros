---
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - docs/MANUAL-SETUP-PHASE1.md
autonomous: true
requirements_addressed:
  - UX-03
---

# Plan 01 — Canonical `{{tagname}}` docs + Phase 1 manual hook/skill setup

## Objective

Align all user-facing specifications with locked decision **D-01** and **D-03** (`{{tagname}}` delimiter, same name rules as UX-03). Add a repo-local manual setup guide for Phase 1 (no `install.js`): registering `UserPromptSubmit` in `~/.claude/settings.json` and copying skill trees to `~/.claude/skills/`.

## Context

- @.planning/phases/01-core-engine/01-CONTEXT.md (D-01, D-03)
- @.planning/ROADMAP.md (Phase 1 success criteria; criterion 2 currently says `<tagname>` — update to `{{tagname}}` to match D-01)
- @.planning/REQUIREMENTS.md (EXPN-01, UX-03 still reference `<tagname>`)

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Docs → implementer | Planning text is trusted; executors must not weaken D-01/D-03 when editing specs. |
| `~/.claude/settings.json` | User-owned; manual doc must warn: only merge hook entries from a trusted repo path; malformed JSON can break Claude Code. |

## STRIDE register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-01-01 | I | ROADMAP / REQUIREMENTS edits | mitigate | Replace `<tagname>` with `{{tagname}}` verbatim where delimiter is meant; keep “lowercase alphanumeric + hyphens, 1–32 chars” unchanged (D-02). |
| T-01-02 | T | Manual setup doc | mitigate | Instruct: use absolute path to **known** repo `hooks/` script; do not `curl` arbitrary URLs into settings. |
| T-01-03 | E | CLAUDE.md | accept | Doc-only; no runtime. |
| T-01-04 | I | User pastes hook snippet | mitigate | Document minimal JSON merge pattern; remind backup of `settings.json`. |
| T-01-05 | D | N/A | accept | No availability target for docs. |
| T-01-06 | R | N/A | accept | No repudiation scope for static docs. |
</threat_model>

## Tasks

<task id="T1">
<objective>Update CLAUDE.md “Tag format” and any inline examples to use `{{tagname}}` while preserving D-02 name rules and stdlib-only constraints (D-03).</objective>
<read_first>
- /home/arau6/projects/pmacros/CLAUDE.md
- /home/arau6/projects/pmacros/.planning/phases/01-core-engine/01-CONTEXT.md
</read_first>
<action>
- In CLAUDE.md project description and “Tag format” bullet, replace angle-bracket `<tagname>` with double-brace `{{tagname}}` wherever the **delimiter** is specified.
- Keep wording: names are lowercase alphanumeric + hyphens, length 1–32 (D-02).
- If “Macro Storage Schema” or examples still say `inline` / `<tagname>`, update delimiter to `{{tagname}}` for consistency with Phase 1.
- Do not add npm dependencies or change non-tag sections unless a sentence becomes false (e.g. “short &lt;tagname&gt; tags” → `{{tagname}}`).
</action>
<acceptance_criteria>
- `grep -n '{{tagname}}' CLAUDE.md` exits 0 and shows at least one hit in the Tag format / project description area.
- `grep -n '<tagname>' CLAUDE.md` exits 1 (no stale delimiter in file), OR only appears inside explicit “legacy/not supported” wording if you add such a note — default is **zero** `<tagname>` as delimiter spec.
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -n '{{tagname}}' CLAUDE.md && ! grep -E '<tagname>' CLAUDE.md
</verify>
</task>

<task id="T2">
<objective>Update REQUIREMENTS.md EXPN-01 and UX-03 to specify `{{tagname}}` delimiter; align wording with D-01/D-02/D-03.</objective>
<read_first>
- /home/arau6/projects/pmacros/.planning/REQUIREMENTS.md
- /home/arau6/projects/pmacros/.planning/phases/01-core-engine/01-CONTEXT.md
</read_first>
<action>
- EXPN-01: Change “`&lt;tagname&gt;` in a prompt” → “`{{tagname}}` in a prompt” (literal braces in markdown).
- UX-03: Change “Tag format is `&lt;tagname&gt;`” → “Tag format is `{{tagname}}`” and retain name grammar sentence.
- Do not change CRUD-03/04 or Phase 2 rows unless a cross-reference explicitly contradicts D-01.
</action>
<acceptance_criteria>
- `grep -n '{{tagname}}' .planning/REQUIREMENTS.md` matches EXPN-01 and UX-03 lines.
- `grep 'EXPN-01' .planning/REQUIREMENTS.md | grep -q '{{tagname}}'`
- `grep 'UX-03' .planning/REQUIREMENTS.md | grep -q '{{tagname}}'`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep 'EXPN-01' .planning/REQUIREMENTS.md | grep '{{tagname}}' && grep 'UX-03' .planning/REQUIREMENTS.md | grep '{{tagname}}'
</verify>
</task>

<task id="T3">
<objective>Update ROADMAP Phase 1 success criterion #2 to `{{tagname}}`; add Phase 1 plan file checklist pointing at this plan set.</objective>
<read_first>
- /home/arau6/projects/pmacros/.planning/ROADMAP.md
- /home/arau6/projects/pmacros/.planning/phases/01-core-engine/01-CONTEXT.md
</read_first>
<action>
- In “### Phase 1: Core Engine”, success criterion 2: replace “Typing `&lt;tagname&gt;`” with “Typing `{{tagname}}`”.
- Set **Plans** to `4` and list unchecked items:
  - `[ ] 01-01-PLAN.md` — docs + manual setup
  - `[ ] 01-02-PLAN.md` — shared lib + tests
  - `[ ] 01-03-PLAN.md` — UserPromptSubmit hook
  - `[ ] 01-04-PLAN.md` — CLI + four skills
- Keep Requirements line unchanged (IDs still valid).
</action>
<acceptance_criteria>
- `grep 'Typing' .planning/ROADMAP.md | grep '{{tagname}}'` exits 0.
- `grep '01-04-PLAN' .planning/ROADMAP.md` exits 0.
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep 'Phase 1: Core Engine' -A25 .planning/ROADMAP.md | grep -q '{{tagname}}' && grep -q '01-04-PLAN' .planning/ROADMAP.md
</verify>
</task>

<task id="T4">
<objective>Add `docs/MANUAL-SETUP-PHASE1.md`: manual registration of UserPromptSubmit and copying skills from repo to `~/.claude/skills/` (Phase 2 will automate).</objective>
<read_first>
- /home/arau6/projects/pmacros/.planning/phases/01-core-engine/01-CONTEXT.md
- https://code.claude.com/docs/en/hooks (UserPromptSubmit, `hookSpecificOutput`)
</read_first>
<action>
Create `docs/MANUAL-SETUP-PHASE1.md` containing:
1. **Paths**: `~/.claude/pmacros/macros.json`, `~/.claude/pmacros/hook-errors.log` (created at runtime).
2. **Hook**: Add a `UserPromptSubmit` entry to `~/.claude/settings.json` whose command runs `node` with the **absolute** path to the repo file `hooks/user-prompt-submit.cjs` (or the path chosen in Plan 03 — use the same filename Plan 03 creates). Note: stdin is the hook JSON event; do not add shell wrappers that consume stdin.
3. **Expected hook stdout** on success: JSON with `hookSpecificOutput.hookEventName === "UserPromptSubmit"` and `hookSpecificOutput.updatedPrompt` set (per **D-04**). On failure: empty stdout, exit 0 (**D-05**).
4. **Skills**: Copy `/.claude/skills/pmacro-add`, `pmacro-list`, `pmacro-preview`, `pmacro-status` from repo to `~/.claude/skills/` (directories with `SKILL.md` inside).
5. **Security note**: Only merge settings from this repo; backup `settings.json` first.
</action>
<acceptance_criteria>
- File exists: `docs/MANUAL-SETUP-PHASE1.md`.
- `grep -q 'UserPromptSubmit' docs/MANUAL-SETUP-PHASE1.md`
- `grep -q 'hookSpecificOutput' docs/MANUAL-SETUP-PHASE1.md`
- `grep -q 'hook-errors.log' docs/MANUAL-SETUP-PHASE1.md`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && test -f docs/MANUAL-SETUP-PHASE1.md && grep -q UserPromptSubmit docs/MANUAL-SETUP-PHASE1.md && grep -q hookSpecificOutput docs/MANUAL-SETUP-PHASE1.md
</verify>
</task>

## Verification (plan-level)

```bash
cd /home/arau6/projects/pmacros && test -f docs/MANUAL-SETUP-PHASE1.md && grep -q '{{tagname}}' CLAUDE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
```

## must_haves (goal-backward from ROADMAP Phase 1)

Derived from Phase 1 success criteria after D-01 alignment:

1. **Truth:** Every canonical spec visible to implementers says macro tags use `{{tagname}}` and D-02 name rules.
2. **Artifact:** `docs/MANUAL-SETUP-PHASE1.md` exists and describes trusted manual hook + skill install for dogfooding without `install.js`.
3. **Link:** ROADMAP lists this phase’s executable plans so `/gsd-execute-phase` can follow file order.
