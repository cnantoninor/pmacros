---
wave: 4
depends_on:
  - 02-03
files_modified:
  - .github/workflows/ci.yml
  - README.md
  - docs/MANUAL-SETUP-PHASE1.md
  - .planning/ROADMAP.md
autonomous: true
requirements_addressed:
  - INST-03
---

# Plan 04 — Cross-platform CI, docs, roadmap plan index

## Objective

Satisfy **INST-03** automated coverage via **GitHub Actions** matrix (`ubuntu-latest`, `macos-latest`) running `npm test` on push/PR. Update **README.md** and **docs/MANUAL-SETUP-PHASE1.md** (or add `docs/INSTALL.md` only if necessary) to document `node install.js`, `--force`, lock file, `--project` flag, and new slash commands. Update **`.planning/ROADMAP.md`** Phase 2 plan checklist to list `02-01`–`02-04` PLAN files.

## Context

- @README.md @.planning/ROADMAP.md
- @.planning/phases/02-full-integration/02-CONTEXT.md

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| CI secrets | Workflow must not echo `HOME` secrets; use only repo checkout. |

## STRIDE register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-04-01 | I | CI cache poisoning | accept | Public repo standard `actions/checkout` only. |
</threat_model>

## Tasks

<task id="T1">
<objective>Add `.github/workflows/ci.yml` with Node LTS matrix Linux + macOS.</objective>
<read_first>
- /home/arau6/projects/pmacros/package.json
</read_first>
<action>
- Trigger: `push`, `pull_request` on default branch.
- Jobs: `test` strategy matrix `os: [ubuntu-latest, macos-latest]`, `node-version: ['20.x']` (or `18.x` + `20.x` if desired — pick one LTS for speed).
- Steps: checkout, `npm test` (no `npm install` if no deps).
</action>
<acceptance_criteria>
- `test -f .github/workflows/ci.yml`
- `grep -q macos-latest .github/workflows/ci.yml`
- `grep -q ubuntu-latest .github/workflows/ci.yml`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && test -f .github/workflows/ci.yml && grep -q npm test .github/workflows/ci.yml
</verify>
</task>

<task id="T2">
<objective>Update `README.md` for Phase 2: install.js, project macros, update/remove, status line.</objective>
<read_first>
- /home/arau6/projects/pmacros/README.md
- /home/arau6/projects/pmacros/.planning/ROADMAP.md
</read_first>
<action>
- Add section **Install (Phase 2)** with `node install.js` and link to manual doc for advanced cases.
- Document `.claude/pmacros/macros.json` project overrides and `pmacro --project` for repo-local CRUD.
- Mention `/pmacro-update` and `/pmacro-remove` alongside existing commands.
- Note **WSL2:** same runtime path as Linux; `ubuntu-latest` CI is treated as covering WSL2 for INST-03; call out one-line manual smoke if users need assurance.
</action>
<acceptance_criteria>
- `grep -q install.js README.md`
- `grep -qE 'pmacro-update|/pmacro-update|pmacro-remove|/pmacro-remove' README.md`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -q install.js README.md && grep -qE 'pmacro-update|pmacro-remove' README.md
</verify>
</task>

<task id="T3">
<objective>Update `docs/MANUAL-SETUP-PHASE1.md` (or add short Phase 2 subsection) for automated install vs manual hook paths.</objective>
<read_first>
- /home/arau6/projects/pmacros/docs/MANUAL-SETUP-PHASE1.md
</read_first>
<action>
- At top or end: **Prefer** `node install.js` for Phase 2+; retain Phase 1 manual steps as fallback.
- Document `statusLine.command` registration outcome from install (reference official status line doc URL).
</action>
<acceptance_criteria>
- `grep -q install.js docs/MANUAL-SETUP-PHASE1.md`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -q install.js docs/MANUAL-SETUP-PHASE1.md
</verify>
</task>

<task id="T4">
<objective>Update `.planning/ROADMAP.md` Phase 2 plan list and plan counts.</objective>
<read_first>
- /home/arau6/projects/pmacros/.planning/ROADMAP.md
</read_first>
<action>
- Set **Plans:** `4 plans` (or equivalent).
- Replace `TBD` under Phase 2 with checklist lines for `02-01-PLAN.md` … `02-04-PLAN.md` matching Phase 1 format (`[ ]` unchecked).
- Set **Status** to `Planned` or keep `Not started` until execute-phase completes (use `Planned` once planning done).
</action>
<acceptance_criteria>
- `grep -q 02-01-PLAN.md .planning/ROADMAP.md`
- `grep -q 02-04-PLAN.md .planning/ROADMAP.md`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -q 02-01-PLAN.md .planning/ROADMAP.md
</verify>
</task>

## Verification (plan-level)

```bash
cd /home/arau6/projects/pmacros && npm test
```

## must_haves (goal-backward)

1. **Truth:** CI runs tests on Linux and macOS — INST-03 (automated slice).
2. **Truth:** New users can discover install + project macros from README — discoverability.
3. **Artifact:** ROADMAP lists all four Phase 2 plans — traceability.
