---
wave: 2
depends_on:
  - 02-01
files_modified:
  - lib/macros-store.cjs
  - scripts/pmacro.cjs
  - test/pmacro-cli.test.cjs
  - .claude/skills/pmacro-update/SKILL.md
  - .claude/skills/pmacro-remove/SKILL.md
autonomous: true
requirements_addressed:
  - CRUD-03
  - CRUD-04
  - STOR-04
---

# Plan 02 — `pmacro update` / `pmacro remove`, merged list/preview, skills

## Objective

Implement **CRUD-03** and **CRUD-04** via `scripts/pmacro.cjs` subcommands with the same validation and atomic write patterns as `add`. Support **user** vs **project** persistence using an explicit **`--project`** flag targeting `.claude/pmacros/macros.json` under `process.cwd()`. Update `list` and `preview` to use **`getMergedMacrosSync(process.cwd())`** so CLI matches hook behavior. Add slash-command skills `pmacro-update` and `pmacro-remove`. Honor **STOR-04**: re-read file immediately before every write on mutation paths.

## Context

- @.planning/phases/02-full-integration/02-CONTEXT.md (D-03, D-04, D-05)
- @scripts/pmacro.cjs @lib/macros-store.cjs
- @.claude/skills/pmacro-add/SKILL.md (pattern)

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv | Tag names validated; values length-capped (`MAX_VALUE_LEN`). |
| Project file | `--project` writes only under resolved `cwd` — user intention; still validate tag names. |

## STRIDE register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-02-04 | T | `removeMacro` wrong file | mitigate | Explicit `--project` flag; default user scope only. |
| T-02-05 | I | Partial write | mitigate | Keep `writeMacrosAtomic` for both scopes. |
</threat_model>

## Tasks

<task id="T1">
<objective>Extend `lib/macros-store.cjs` with scope-targeted read/write helpers for update/remove.</objective>
<read_first>
- /home/arau6/projects/pmacros/lib/macros-store.cjs
- /home/arau6/projects/pmacros/lib/paths.cjs
</read_first>
<action>
- Add `readMacrosAtPath(macrosPath)` — may throw on invalid JSON (CLI surfaces errors) **or** reuse strict read for CLI-only path parameter.
- Add `writeMacrosAtomicAtPath(macrosPath, data)` — same temp+rename as user path but parameterized directory from `macrosPath`.
- Add `updateMacroAtPath({ macrosPath, tag, value, description })` and `removeMacroAtPath({ macrosPath, tag })`: **read current file** → mutate → `writeMacrosAtomicAtPath`; throw `code: 'NOT_FOUND'` if tag missing on remove; for update require existing tag or document upsert behavior — **require existing tag** for `update`, else exit error (CRUD-03 = update existing).
- Export helpers used by CLI.
</action>
<acceptance_criteria>
- `grep -q removeMacroAtPath lib/macros-store.cjs`
- `grep -q writeMacrosAtomicAtPath lib/macros-store.cjs`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -q removeMacroAtPath lib/macros-store.cjs
</verify>
</task>

<task id="T2">
<objective>Implement `pmacro update`, `pmacro remove`, merged `list`/`preview`, and `--project` flag parsing.</objective>
<read_first>
- /home/arau6/projects/pmacros/scripts/pmacro.cjs
- /home/arau6/projects/pmacros/lib/macros-store.cjs
</read_first>
<action>
- Usage: `pmacro update [--project] <tag> <value> [description...]` and `pmacro remove [--project] <tag>`.
- When `--project`: `macrosPath = getProjectMacrosPath(process.cwd())`; else `getMacrosPath()`.
- `list`: print merged table via `getMergedMacrosSync(process.cwd())`; indicate source column optional — if too wide, append `(merged)` in header only.
- `preview`: expand using merged `macros` from `getMergedMacrosSync(process.cwd())`.
- Update default `usage:` string in `die` for unknown command.
</action>
<acceptance_criteria>
- `grep -q "case 'update'" scripts/pmacro.cjs`
- `grep -q "case 'remove'" scripts/pmacro.cjs`
- `grep -q getMergedMacrosSync scripts/pmacro.cjs`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && grep -q "case 'update'" scripts/pmacro.cjs && grep -q "case 'remove'" scripts/pmacro.cjs
</verify>
</task>

<task id="T3">
<objective>Extend `test/pmacro-cli.test.cjs` for update/remove and merged preview.</objective>
<read_first>
- /home/arau6/projects/pmacros/test/pmacro-cli.test.cjs
</read_first>
<action>
- With isolated `HOME` and temp project dir: create user + project macros; assert `preview` shows project override; `remove --project` removes project tag only; `update` changes user macro.
- Assert `update` fails with non-zero exit when tag missing (subprocess `status !== 0`).
</action>
<acceptance_criteria>
- `npm test` exits 0
- `grep -q update test/pmacro-cli.test.cjs`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && npm test
</verify>
</task>

<task id="T4">
<objective>Add `pmacro-update` and `pmacro-remove` skills under `.claude/skills/`.</objective>
<read_first>
- /home/arau6/projects/pmacros/.claude/skills/pmacro-add/SKILL.md
- /home/arau6/projects/pmacros/docs/MANUAL-SETUP-PHASE1.md
</read_first>
<action>
- Create `.claude/skills/pmacro-update/SKILL.md` and `.claude/skills/pmacro-remove/SKILL.md` with YAML frontmatter: `name: pmacro-update` / `pmacro-remove`, `disable-model-invocation: true`, descriptions for when to use.
- Body: instruct running `node <REPO>/scripts/pmacro.cjs update …` / `remove …`, optional `--project`, and AskUserQuestion flow mirroring add skill style.
</action>
<acceptance_criteria>
- `test -f .claude/skills/pmacro-update/SKILL.md`
- `test -f .claude/skills/pmacro-remove/SKILL.md`
- `grep -q pmacro.cjs .claude/skills/pmacro-update/SKILL.md`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && test -f .claude/skills/pmacro-update/SKILL.md && test -f .claude/skills/pmacro-remove/SKILL.md
</verify>
</task>

## Verification (plan-level)

```bash
cd /home/arau6/projects/pmacros && npm test
```

## must_haves (goal-backward)

1. **Truth:** `/pmacro-update` and `/pmacro-remove` skills exist and invoke CLI — CRUD-03, CRUD-04.
2. **Truth:** CLI `list`/`preview` reflect merged effective macros — aligns with STOR-02 UX.
3. **Truth:** Mutations re-read before write — STOR-04.
