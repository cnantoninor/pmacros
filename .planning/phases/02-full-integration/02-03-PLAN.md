---
wave: 3
depends_on:
  - 02-02
files_modified:
  - install.js
  - scripts/pmacro-statusline.cjs
  - test/install.test.cjs
  - test/pmacro-statusline.test.cjs
  - .gitignore
autonomous: true
requirements_addressed:
  - INST-01
  - INST-02
  - UX-01
---

# Plan 03 — `install.js`, skill backup copy, status line script, lock file

## Objective

Implement **INST-01** (idempotent hook + skill install), **INST-02** (backup differing `SKILL.md` before overwrite), **UX-01** (stdlib-only status line script printing merged tag names; `install.js` registers `statusLine.command` in `~/.claude/settings.json`). Add `.claude/.pmacros-installed` lock file in the project (**D-08**) with `--force` bypass. Stdlib only — no npm deps.

## Context

- @.planning/phases/02-full-integration/02-CONTEXT.md (D-06–D-11)
- @docs/MANUAL-SETUP-PHASE1.md (current hook registration shape)
- @lib/macros-store.cjs `getMergedMacrosSync`

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| `~/.claude/settings.json` | User-owned; installer must merge without destroying unrelated keys. |
| Skill sources | Read from repo only; validate paths stay under repo `hooks/` and `.claude/skills/`. |

## STRIDE register

| ID | Category | Component | Disposition | Mitigation |
|----|----------|-----------|-------------|------------|
| T-03-01 | A | settings merge | mitigate | Parse JSON; deep-merge only known keys (`hooks`, `statusLine`); write temp + rename for settings file. |
| T-03-02 | T | Path traversal | mitigate | Resolve repo root as `path.resolve(__dirname)`; reject `..` in skill names when walking directory. |
</threat_model>

## Tasks

<task id="T1">
<objective>Create `scripts/pmacro-statusline.cjs`: one-line merged tag listing, always exit 0.</objective>
<read_first>
- /home/arau6/projects/pmacros/lib/macros-store.cjs
- /home/arau6/projects/pmacros/.planning/phases/02-full-integration/02-RESEARCH.md
</read_first>
<action>
- Shebang `#!/usr/bin/env node`, `'use strict'`.
- `const cwd = process.cwd()`; `const { macros } = getMergedMacrosSync(cwd)`; tags = `Object.keys(macros).sort()`; join with single space; if length > 120, truncate to 117 + `…`.
- Wrap in try/catch: on any error print empty line or `pmacros` and exit 0.
- Do not print macro values.
</action>
<acceptance_criteria>
- `test -f scripts/pmacro-statusline.cjs`
- `grep -q getMergedMacrosSync scripts/pmacro-statusline.cjs`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && test -f scripts/pmacro-statusline.cjs && grep -q getMergedMacrosSync scripts/pmacro-statusline.cjs
</verify>
</task>

<task id="T2">
<objective>Implement repo-root `install.js` (stdlib only): lock file, settings merge, skill copy with backup.</objective>
<read_first>
- /home/arau6/projects/pmacros/docs/MANUAL-SETUP-PHASE1.md
- /home/arau6/projects/pmacros/.claude/skills/pmacro-add/SKILL.md
</read_first>
<action>
- **Args:** `--force` optional.
- **Repo root:** `path.join(__dirname)`; hook abs path `path.join(repoRoot, 'hooks', 'user-prompt-submit.cjs')`; statusline abs path `path.join(repoRoot, 'scripts', 'pmacro-statusline.cjs')`.
- **Lock:** If `path.join(repoRoot, '.claude', '.pmacros-installed')` exists and not `--force`, print message and exit 0 early **without** changing `~/.claude` (idempotent skip) — or per D-08: skip redundant work; document behavior in plan README task.
- **Settings:** Read `~/.claude/settings.json`; if missing, start `{}`. Ensure `hooks.UserPromptSubmit` is array; append hook command object **only if** no element contains substring `user-prompt-submit.cjs` matching this repo’s path (normalize slashes for compare). Set `statusLine.command` to `"node "` + JSON-stringify-friendly absolute path to statusline script (exact format Claude expects — match MANUAL pattern if documented).
- **Atomic settings write:** temp file in same dir as `settings.json`, then `renameSync`.
- **Skills:** Glob `repoRoot/.claude/skills/pmacro-*/SKILL.md`; for each, `mkdirSync` dest `~/.claude/skills/<sameDirName>/` recursive; if dest `SKILL.md` exists and content !== source, copy dest to `SKILL.md.pmacros-backup.<ISO-timestamp>`; then `copyFileSync` source → dest.
- After success, write lock file with timestamp comment or minimal JSON `{ "version": 1 }`.
- Add `.gitignore` line `.claude/.pmacros-installed` if not present.
</action>
<acceptance_criteria>
- `test -f install.js`
- `grep -q UserPromptSubmit install.js`
- `grep -q statusLine install.js`
- `grep -q pmacros-backup install.js` || `grep -q backup install.js`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && test -f install.js && grep -q UserPromptSubmit install.js
</verify>
</task>

<task id="T3">
<objective>Add `test/install.test.cjs` and `test/pmacro-statusline.test.cjs` using fake `HOME` and temp repo layout.</objective>
<read_first>
- /home/arau6/projects/pmacros/test/pmacro-cli.test.cjs
</read_first>
<action>
- **install test:** Copy or symlink minimal structure: point `HOME` at temp; create fake empty `settings.json` or missing; run `node /abs/install.js` with `cwd` = temp repo containing `.claude/skills/pmacro-add/SKILL.md`; assert `settings.json` gains hook entry; run second time assert no duplicate hook lines (parse JSON array length); use `--force` to re-run when lock present.
- **statusline test:** Subprocess `node scripts/pmacro-statusline.cjs` with `cwd` set to project with merged tags; assert stdout matches sorted tags separated by spaces.
- All tests must not touch real user home.
</action>
<acceptance_criteria>
- `npm test` exits 0
- `test -f test/install.test.cjs`
</acceptance_criteria>
<verify>
cd /home/arau6/projects/pmacros && npm test
</verify>
</task>

## Verification (plan-level)

```bash
cd /home/arau6/projects/pmacros && npm test
```

## must_haves (goal-backward)

1. **Truth:** `node install.js` merges hook + status line without duplicating entries on second run — INST-01.
2. **Truth:** Differing skill files get a `.pmacros-backup.*` (or documented INST-02 pattern) before overwrite — INST-02.
3. **Truth:** Status script prints tag names from merged set only — UX-01.
